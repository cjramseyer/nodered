'use strict';

function _inheritsLoose(subClass, superClass) { subClass.prototype = Object.create(superClass.prototype); subClass.prototype.constructor = subClass; subClass.__proto__ = superClass; }

var CalBengali = require('date-bengali-revised').default;

var CalEvent = require('./CalEvent');

var CalDate = require('caldate');

var BengaliRevised = /*#__PURE__*/function (_CalEvent) {
  _inheritsLoose(BengaliRevised, _CalEvent);

  /**
   * @param {object} [opts]
   */
  function BengaliRevised(opts) {
    opts = opts || {};
    return _CalEvent.call(this, opts) || this;
  }

  var _proto = BengaliRevised.prototype;

  _proto.inYear = function inYear(year) {
    var opts = this.opts;
    var date = new CalBengali(year - 593, opts.month, opts.day).toGregorian();
    var d = new CalDate(date);
    this.dates.push(d);
    return this;
  };

  return BengaliRevised;
}(CalEvent);

module.exports = BengaliRevised;