'use strict';

function _inheritsLoose(subClass, superClass) { subClass.prototype = Object.create(superClass.prototype); subClass.prototype.constructor = subClass; subClass.__proto__ = superClass; }

var CalEventMap = require('./CalEventMap');

var calendar = require('./internal/hebrew-calendar');

var Hebrew = /*#__PURE__*/function (_CalEventMap) {
  _inheritsLoose(Hebrew, _CalEventMap);

  function Hebrew(opts) {
    var _this;

    _this = _CalEventMap.call(this, opts) || this;
    _this.calendar = calendar;
    return _this;
  }

  var _proto = Hebrew.prototype;

  _proto.get = function get(timezone) {
    var _this2 = this;

    var arr = this.dates.map(function (date) {
      var o = {
        date: date.toString() + ' -0600',
        start: date.setOffset(-6, 'h').toTimezone(timezone),
        end: date.toEndDate().toTimezone(timezone)
      };

      _this2._addSubstitute(date, o);

      return o;
    });
    return arr;
  };

  return Hebrew;
}(CalEventMap);

module.exports = Hebrew;